{
  "private": true,
  "scripts": {
    "test": "trunk build --release && vitest run --browser.enabled"
  },
  "devDependencies": {
    "@vitest/browser-playwright": "^4.1.11",
    "playwright": "^1.63.0",
    "vitest": "^5.0.0"
  }
}
